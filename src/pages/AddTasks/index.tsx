import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const AddTasksPage = (props: Props) => {
	return (
		<View>
			<Text>AddTasksPage</Text>
		</View>
	);
};

export default AddTasksPage;

const styles = StyleSheet.create({});
